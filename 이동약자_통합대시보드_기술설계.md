# 이동약자 이동성+보행안전 통합 대시보드

> 이 문서 하나만 읽어도 프로젝트 전체를 이해할 수 있도록 작성했습니다. 엑셀 비교표나 다른 설계 문서를 먼저 볼 필요는 없습니다.
> 더 깊은 기술 디테일이 필요하면 문서 끝의 "부록: 더 읽을 자료"를 참고하세요.

## 1. 배경 — 왜 이 프로젝트인가

이동약자(장애인+고령자)는 국내 인구의 30.9%(약 1,586만 명)에 달하지만, 이들의 이동·보행 문제는 크게 두 갈래로 나뉩니다.

1. **이동 문제**: 장애인콜택시(특별교통수단)의 대기시간이 길고, 지역별 편차가 큽니다. 이 편차가 저상버스 보급률 같은 인프라 격차와 관련 있는지는 검증된 적이 없습니다.
2. **보행 문제**: 보도의 턱낮춤·점자블록 같은 접근성 시설이 지역마다 다르고, 고령 보행자 교통사고도 계속 늘고 있습니다(2025년 7,301건 → 11,498건). 기존 서비스(서울동행맵 등)는 정보가 부정확하다는 지적을 받고 있습니다.

이 프로젝트는 위 두 문제를 각각 딥러닝으로 분석하고, **하나의 지도 대시보드**로 합쳐서 "어느 지역을 가장 먼저 개선해야 하는가"를 한눈에 보여줍니다.

## 2. 한 문장 요약

자치구(구)별로 **① 장애인콜택시를 얼마나 오래 기다려야 하는지, ② 보도 접근성이 얼마나 좋은지, ③ 고령 보행자 사고 위험이 얼마나 높은지**를 각각 딥러닝으로 예측한 뒤, 이 세 지표를 하나의 지도에 겹쳐서 우선 개선 지역을 짚어주는 웹 대시보드를 만든다.

## 3. 왜 세 개를 따로 만들고 나중에 합치는가

이동 문제는 **시계열 예측**, 접근성은 **이미지 인식(CV)**, 사고위험은 **정형 데이터 회귀**로 풀어야 해서 기법이 완전히 다릅니다. 이 셋을 하나의 파이프라인으로 억지로 합치면 개발·검증 시간이 배로 늘어나 2일 안에 끝내기 어렵습니다.

그래서:
- 세 문제를 **각각 독립적으로** 학습·완성한다.
- 결합은 오직 **마지막 시각화 단계**에서, 각 모듈의 출력값을 자치구 단위로 정규화해 같은 지도에 겹쳐 보여주는 방식으로만 이뤄진다.
- 이렇게 하면 한 모듈이 늦어져도 다른 모듈에 영향을 주지 않는다.

## 4. 3개 모듈 상세

### 모듈A — 콜택시 수요·형평성 예측

- **문제**: 장애인콜택시 대기시간이 지역마다 왜 다른가? 저상버스가 적은 지역일수록 콜택시 의존도(=대기시간)가 높은가?
- **입력 데이터**: 장애인콜택시 일별 이용현황(일자·지역·콜 건수·대기시간), 기상 데이터, 요일/공휴일, 저상버스 보급률(지역별)
- **방법**:
  1. 1단계 — 시계열 딥러닝(TFT, PatchTST, N-BEATS)으로 지역별 수요·대기시간을 예측하고, 전통 ML(XGBoost/LightGBM)과 성능 비교
  2. 2단계 — 예측된 대기시간 격차와 저상버스 보급률의 관계를 XGBoost+SHAP으로 분석해 "저상버스가 부족한 지역일수록 얼마나 더 오래 기다리는가"를 수치로 설명
- **출력**: 자치구별 "대기시간 격차 지수"

### 모듈B — 보행 접근성 탐지

- **문제**: 어느 지역의 보도가 턱낮춤·점자블록 등 접근성 시설을 잘 갖추고 있는가?
- **입력 데이터**: 보행로 사진(AI허브 "보행 안전을 위한 도로 시설물 데이터", 약 102.5만 장)
- **방법**: YOLOv8/v11 또는 SAM으로 턱낮춤·점자블록·경사로 등을 이미지에서 탐지 → 지점별 접근성 점수 산출
- **출력**: 자치구별 "접근성 점수"

### 모듈C — 고령 보행자 사고위험 예측

- **문제**: 어느 지역이 고령 보행자에게 특히 위험한가?
- **입력 데이터**: 지역별 교통사고 통계(연령대별), 노인복지시설 밀도, 인프라 변수
- **방법**: XGBoost/TabNet으로 지역별 위험지수를 예측(공개된 완성 지표가 없어 자체 설계) + SHAP으로 어떤 요인이 위험을 높이는지 해석
- **출력**: 자치구별 "사고위험지수"

## 5. 전체 흐름도

```
[모듈A] 콜택시 데이터 + 기상 + 저상버스 보급률
        → 시계열 예측 → SHAP 해석
        → 자치구별 "대기시간 격차 지수"

[모듈B] 보행로 사진
        → YOLO/SAM 탐지
        → 자치구별 "접근성 점수"

[모듈C] 사고통계 + 노인복지시설 밀도
        → XGBoost/TabNet 예측
        → 자치구별 "사고위험지수"

[결합]  세 지수를 자치구 단위로 정규화
        → Streamlit/Folium 지도 대시보드
        → ① 이동성 탭  ② 보행안전 탭  ③ 종합 우선순위 탭
```

## 6. 결합(대시보드) 단계 상세

- 세 모듈의 출력은 이미 자치구 단위(또는 자치구로 집계 가능한 단위)이므로, 자치구 코드를 공통 키로 삼아 병합한다.
- **종합 우선순위 점수** 계산식(예시): `종합점수 = 대기시간격차지수 × w1 + (1-접근성점수) × w2 + 사고위험지수 × w3` — 가중치는 팀 논의로 정하되, 근거가 약하면 동일 가중치(1/3씩)를 기본값으로 쓰고 민감도 분석을 곁들인다.
- 대시보드는 3개 탭으로 구성한다: **이동성 탭**(모듈A만), **보행안전 탭**(모듈B+C), **종합 탭**(세 지수 오버레이). 시간이 부족하면 종합 탭만 축소하고 앞의 두 탭만 제출해도 완결된 결과물이 된다.

## 7. 결합에만 별도로 필요한 데이터

모듈 A/B/C 각각의 데이터는 위 4절에서 이미 확보 가능한 것으로 확인됐지만, **자치구 단위로 지도에 겹쳐 보여주는 결합 단계 자체**에는 아래 3종의 데이터가 추가로 필요합니다.

| 필요한 이유 | 데이터 | 확보 경로 |
|---|---|---|
| 지도에 자치구별로 색칠(코로플레스)하려면 자치구의 지리적 경계(폴리곤)가 필요 | 자치구 GIS 경계(GeoJSON) | GitHub `datainworld/administrative_district` (서울시 자치구 25개, 즉시 사용 가능) — https://github.com/datainworld/administrative_district |
| 세 모듈의 출력 단위가 제각각(콜택시: 자치구/행정동, CV: 좌표, 사고통계: 시군구)이라 하나로 합치려면 공통 코드가 필요 | 행정표준코드(법정동코드) | 행정표준코드관리시스템 — https://www.code.go.kr/stdcode/regCodeL.do |
| 인구가 많은 구가 지수 비교에서 항상 불리해지지 않도록 인구(또는 고령인구) 대비로 정규화 | 자치구별/연령별 인구 통계 | KOSIS 행정구역(시군구)별/1세별 주민등록인구(DT_1B04006) — https://kosis.kr/statHtml/statHtml.do?orgId=101&tblId=DT_1B04006 |

추가로, 모듈B의 CV 탐지 결과(개별 좌표)를 자치구 단위로 집계하려면 GeoPandas 등을 이용한 **공간 조인(spatial join)** 처리 단계가 파이프라인에 별도로 필요합니다. 위 데이터는 통합·대시보드팀이 D-day 이전에 다른 팀 작업과 병렬로 미리 확보해두는 것을 권장합니다.

## 8. 4인 역할 분담

| 역할 | 담당 인원 | 하는 일 |
|---|---|---|
| 수요예측팀 | 1명 | 모듈A(콜택시 수요·형평성) 전담 |
| CV팀 | 1명 | 모듈B(접근성 탐지) 전담 |
| 정형데이터팀 | 1명 | 모듈C(사고위험 예측) 전담 |
| 통합·대시보드팀 | 1명 | 3개 모듈 출력을 자치구 단위로 병합, Streamlit 대시보드 구축 |

## 9. 최종 산출물

**모듈별**
- 모듈A: RMSE/MAE 비교표(통계모델 vs 딥러닝) + 지역별 SHAP summary plot(저상버스 영향력 시각화) + 형평성 우선순위 리스트
- 모듈B: 접근성 점수 지도 + mAP·추론속도(FPS) 성능 지표
- 모듈C: 사고위험지수 예측 성능(RMSE/MAE) + SHAP summary plot

**통합 (14번 고유 산출물)**
- Streamlit/Folium 3탭 대시보드(이동성 / 보행안전 / 종합 우선순위)
- 자치구별 종합 우선순위 지도
- 정책 제언 — 형평성 개선(모듈A)과 인프라 우선순위(모듈B+C)를 하나의 결론으로 연결한 최종 리포트

## 10. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 통합·대시보드팀에 마지막 단계가 몰려 병목이 될 수 있음 | 각 모듈은 늦어도 D-day 하루 전까지 "자치구코드+지수값" CSV 포맷을 고정해, 통합팀이 미리 대시보드 뼈대를 만들 수 있게 한다 |
| 모듈B의 AI허브 이미지 데이터 승인이 지연될 수 있음 | Roboflow "accessibility street", Kaggle "Sidewalk Segment Dataset"/"SF Curb Ramps"(모두 즉시 다운로드 가능, 검증 완료)로 대체 |
| 종합점수 가중치(w1,w2,w3)에 객관적 근거가 부족할 수 있음 | 민감도 분석을 곁들이거나, 근거가 약하면 동일 가중치(1/3씩)를 기본값으로 명시 |
| 모듈C의 사고통계를 TAAS 웹 화면에서 일일이 조회하면 시간이 오래 걸림 | TAAS 대신 공식 Open API(`opendata.koroad.or.kr`, 노인보행자 등 대상별 세분화 통계 제공)로 대체 |

---

## 부록: 더 읽을 자료

- 모듈A(콜택시 수요예측)의 상세 설계: `장애인콜택시_수요예측_기술설계.md`
- 모듈B+C(보행 접근성·사고위험)의 상세 설계: `보행약자_안전접근성_기술설계.md`
- 이 프로젝트가 나오기까지의 논의 과정: `이동약자_이동성_프로젝트_개요.md` 4.6절
- 전체 14개 후보 아이디어 비교: `딥러닝_프로젝트_종합비교표.xlsx` (이 프로젝트는 O열, "14번")
