# 이동약자 이동성+보행안전 통합 대시보드 — 기술 설계 문서

> `딥러닝_프로젝트_종합비교표.xlsx` 14번(9+13 결합, O열) 아이디어의 기술 설계안.
> 배경·결합 방식 논의는 `이동약자_이동성_프로젝트_개요.md` 4.6절 참고.
> 개별 모듈 상세 설계는 `장애인콜택시_수요예측_기술설계.md`(9번), `보행약자_안전접근성_기술설계.md`(13번) 참고 — 이 문서는 두 결과를 결합하는 방식만 다룬다.

## 1. 목표

장애인콜택시 수요예측·형평성 분석(9번)과 보행약자 접근성·사고위험 분석(13번)을 각각 독립 모듈로 완성한 뒤, 하나의 대시보드에서 자치구 단위로 오버레이해 "이동(콜택시 의존도)"과 "보행(접근성·사고위험)" 두 축을 함께 보여주는 통합 서비스를 만든다.

## 2. 결합 원칙 — 모델은 합치지 않는다

세 가지 모델링 기법(시계열, CV, 정형데이터)을 하나의 파이프라인으로 강제 결합하면 개발·검증 시간이 배로 늘어나 2일 일정 안에 완결하기 어렵다. 따라서:

- 9번(TFT/PatchTST 수요예측 + XGBoost/SHAP 형평성)과 13번(YOLO/SAM 접근성 + XGBoost/TabNet 사고위험)을 **각각 독립적으로 학습·완성**한다.
- 결합은 오직 **마지막 시각화 단계**에서, 각 모듈의 출력값(수요/대기시간 격차, 접근성 점수, 사고위험지수)을 자치구 단위로 정규화해 같은 지도에 레이어로 얹는 방식으로만 이뤄진다.
- 이렇게 하면 9번의 "상" 실현성과 13번의 "중" 실현성이 서로의 리스크를 전이시키지 않는다.

## 3. 전체 구조

```
[모듈A - 수요예측/형평성, 9번 그대로 재사용]
콜택시 이용현황 + 기상 + 저상버스 보급률
→ TFT/PatchTST(1단계) → XGBoost+SHAP(2단계)
→ 자치구별 "대기시간 격차 지수"

[모듈B - 접근성, 13번 모듈1 그대로 재사용]
보행로 이미지 → YOLOv8/SAM → 접근성 점수화
→ 자치구별 "접근성 점수"

[모듈C - 사고위험, 13번 모듈2 그대로 재사용]
사고통계 + 노인복지시설 밀도 → XGBoost/TabNet
→ 자치구별 "사고위험지수"

[결합 - 이 문서의 범위]
세 지수를 자치구 단위로 정규화 → Streamlit/Folium 대시보드
→ 이동(수요·형평성) 탭 + 보행(접근성·사고위험) 탭 + 종합 우선순위 탭
```

## 4. 결합 단계 상세

- 세 모듈 출력은 이미 자치구 단위(또는 자치구로 집계 가능한 단위)이므로 별도 공간 조인 없이 자치구 코드 기준으로 병합한다.
- 종합 우선순위 점수 예시: `종합점수 = 대기시간격차지수 × w1 + (1-접근성점수) × w2 + 사고위험지수 × w3` (가중치는 팀 논의로 결정, 발표 시 민감도 분석 권장)
- 대시보드는 3개 탭으로 구성: (1) 이동성 탭 - 9번 결과 단독, (2) 보행안전 탭 - 13번 결과 단독, (3) 종합 탭 - 세 지수 오버레이. 시간이 부족하면 3번 탭만 축소하고 1·2번 탭까지만 제출해도 완결된 결과물이 된다.

## 5. 역할 분담 (4인 기준)

| 역할 | 담당 | 재사용 범위 |
|---|---|---|
| 수요예측팀 | 1명 | 9번 파이프라인 그대로 |
| CV팀 | 1명 | 13번 모듈1 그대로 |
| 정형데이터팀 | 1명 | 13번 모듈2 그대로 |
| 통합/대시보드팀 | 1명 | 3개 모듈 출력을 자치구 단위로 병합, Streamlit 대시보드 구축 |

## 6. 산출물

- 모듈별 산출물은 9번·13번 문서와 동일 (RMSE/MAE 비교표, SHAP plot, mAP/접근성 지도, 사고위험 예측 성능)
- 통합 대시보드: 자치구별 종합 우선순위 지도 + 정책 제언(형평성 개선 + 인프라 우선순위를 하나의 결론으로 연결)

## 7. 리스크

- 통합 단계가 4번째 역할(통합/대시보드팀)에 집중되므로, 다른 3팀의 산출물이 늦어지면 병목이 됨 → 각 모듈은 늦어도 D-day 하루 전까지 자치구 단위 출력 포맷(CSV: 자치구코드, 지수값)을 고정해 통합팀이 미리 대시보드 뼈대를 만들 수 있게 함
- CV 모듈(13번 모듈1) 승인 지연 리스크는 그대로 유지 — 지연 시 Roboflow/Kaggle 대체 데이터로 축소
- 3개 지수의 가중치(w1,w2,w3)에 객관적 근거가 부족할 수 있음 → 민감도 분석 또는 동일 가중치(1/3씩) 사용을 기본안으로 명시
