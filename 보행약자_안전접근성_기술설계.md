# 보행약자 안전·접근성 통합 서비스 — 기술 설계 문서

> `딥러닝_프로젝트_종합비교표.xlsx` 13번(10+11 결합, N열) 아이디어의 기술 설계안.
> 배경·데이터 재조사 내용은 `이동약자_이동성_프로젝트_개요.md` 4.5절 참고.

## 1. 목표

장애인·고령자를 아우르는 "보행약자"를 대상으로, (1) 보행로 접근성 시설물(턱낮춤·점자블록·경사로)의 유무·상태를 자동 탐지하고 (2) 지역별 고령 보행자 사고위험을 예측해, 두 결과를 하나의 지도에 결합함으로써 "접근성이 낮으면서 사고위험도 높은" 지역을 우선순위로 짚어낸다.

## 2. 전체 구조

```
[모듈 1: 접근성 탐지 (CV)]
보행로 이미지 → YOLOv8/v11 또는 SAM 세그멘테이션
→ 턱낮춤·점자블록·경사로·계단 등 탐지 → 좌표 매핑 → 접근성 점수화

[모듈 2: 사고위험 예측 (Tabular)]
지역별 사고통계(연령대별) + 노인복지시설 밀도 + 인프라 변수
→ XGBoost/TabNet 회귀로 위험지수 예측 (자체 설계, 선행연구 방법론 참고) + SHAP 해석

[결합]
모듈1 접근성 점수 + 모듈2 위험지수 → 자치구/격자 단위로 오버레이
→ Folium/Streamlit 지도 대시보드 (우선순위 지역 시각화)
```

## 3. 모듈별 상세

### 3.1 모듈 1 — 접근성 탐지 (Computer Vision)

- 모델: YOLOv8/v11 객체탐지 또는 SAM 세그멘테이션
- 입력: AI허브 "보행 안전을 위한 도로 시설물 데이터" (약 102.5만 장, 안전·관리·환경·편의시설 유형별 양호/불량 판정 포함)
- 출력: 지점별 접근성 점수 (턱낮춤/점자블록 존재 여부·상태를 가중합)
- 리스크: AI허브 데이터 승인 대기 시간 → 지연 시 대체 데이터로 전환(검증 완료, 즉시 다운로드 가능)
  - Roboflow Universe "accessibility street"(ramps-curbs 4,968장, 프리트레인 모델 포함): https://universe.roboflow.com/tfg-7qtpm/accesibility-street
  - Kaggle "Sidewalk Segment Dataset"(VOC2007 포맷): https://www.kaggle.com/datasets/lemnix/sidewalk-segment-dataset
  - Kaggle "SF Curb Ramps": https://www.kaggle.com/datasets/san-francisco/sf-curb-ramps

### 3.2 모듈 2 — 고령 보행자 사고위험 예측 (Tabular)

- 모델: XGBoost/LightGBM 베이스라인 + TabNet, SHAP으로 해석
- 입력 피처:
  - 도로교통공단_시군구별 월별 교통사고 통계 (https://www.data.go.kr/data/15070295/fileData.do)
  - 서울시 교통사고 현황(연령층별 사상자) (https://data.seoul.go.kr/dataList/10754/S/2/datasetView.do)
  - 서울시 보행 교통사고 현황 (https://data.seoul.go.kr/dataList/324/S/2/datasetView.do)
  - 보건복지부_노인복지 시설현황 (https://www.data.go.kr/data/15039034/fileData.do)
  - (권장, 신규 확인) KOROAD 교통사고정보 개방체계 Open API — 지자체별 대상사고통계(시군구 단위 + "노인보행자" 등 대상별 세분화 통계를 API로 즉시 조회, 회원가입+API키 발급 필요): https://opendata.koroad.or.kr/api/selectSttDataSet.do
- 타깃: 자체 설계 위험지수 (사고건수·고령자 비중을 정규화해 산출, 선행연구의 PSI 방법론 참고)
- 참고 위험요인(선행연구): 고령자 통행량↑·주거지역 면적↑·연평균일교통량↑ → 위험 증가 / 노인여가복지시설 수↑ → 위험 감소

### 3.3 결합 단계

- 두 모듈의 출력을 동일한 지역 단위(자치구 또는 격자)로 정규화 후 결합 점수 산출
- 예: `우선순위 점수 = (1 - 접근성 점수) × w1 + 위험지수 × w2`
- Folium/Streamlit으로 지도 시각화, 색상으로 우선순위 표현

## 4. 데이터 정합 이슈

- 모듈1(CV)은 지점 좌표 단위, 모듈2(사고통계)는 자치구/시군구 단위 → 결합 시 CV 탐지 결과를 자치구로 집계해야 함
- AI허브 라벨 단위가 서비스에 필요한 수준(개별 지점 좌표)인지 사전 확인 필요
- 2일 일정 고려 시 전국이 아닌 **자치구 1곳으로 파일럿 범위 한정** 권장

## 5. 산출물

- 모듈1: 접근성 점수 지도 (mAP, 추론속도 FPS 포함)
- 모듈2: 사고위험지수 예측 성능(RMSE/MAE, 통계모델 대비 비교) + SHAP summary plot
- 결합 대시보드: 우선순위 지역 리스트 + 정책 제언

## 6. 리스크

- AI허브 승인 지연 시 CV 모듈만 대체 데이터(3.1절 Roboflow/Kaggle, 검증 완료)로 축소 진행
- 두 모듈을 4인이 병렬(CV팀/정형데이터팀)로 진행해도 결합 단계(지도 오버레이)에서 시간 압박 가능
- PSI 원지표는 비공개라 자체 설계 위험지수의 타당성을 어떻게 방어할지 사전 논의 필요
